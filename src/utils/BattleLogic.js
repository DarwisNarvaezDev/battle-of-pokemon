export class BattleLogic {
  constructor(){};

  /**
   * Gets the data of the battle from session storage and unifies it
   * 
   */
  static startBattle(){
    try{
      
    }catch(error){
      console.error(error);
    }
  }
}